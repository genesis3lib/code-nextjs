import type { NextConfig } from "next";

const nextConfig: NextConfig = {
{{#isStaticMode}}
  // Static export for S3/CloudFront deployment
  output: 'export',

  // Disable image optimization for static export
  images: {
    unoptimized: true,
  },

  // Trailing slash for S3 compatibility
  trailingSlash: true,
{{/isStaticMode}}
{{#isSsrMode}}
  // SSR mode - standalone output for EC2 deployment
  output: 'standalone',

  // Enable image optimization (requires server)
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
{{/isSsrMode}}
};

export default nextConfig;
